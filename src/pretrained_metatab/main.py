import sys



def main():
    ''''''
    """
    pars = parse_args(sys.argv[1:])
    data_loader = DataLoader()
    model = pickle(......)

    pred_proba = model.predict_proba()

    1) ricavare informazione di preprocessing dal modello.
    Si potrebbe passare in input un dizionario con le info relative al preprocessing
    e alla natura del modello (che roba è) e al risultato dell'eventuale ottimizazzione (?)
    ---> questo dizionario sarebbe da generare nell'altro programma.
    
    2) Costruire il prediction dataframe con pred_proba e info da 1.
    
    3) Salvare il prediction dataframe.
    """